// Enumerations are user-defined types that consist of a set of named integer constants.
// They are useful when you have a fixed set of values that are mutually exclusive.
enum Status
{
    Pending,
    Approved,
    Cancelled
};